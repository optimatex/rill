import { React } from '../src/_employee/view';

export const renderImages = (el) => <img />;

export const renderVideos = (el) => <iframe />;

export const mockCombinedRenderProps = {
  renderImages,
  renderVideos,
};
