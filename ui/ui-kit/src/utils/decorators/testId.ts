import { makeTestId } from './testIdOperations';

export const [layoutIdKey, layoutId] = makeTestId('layout');

export const [columnIdKey, columnId] = makeTestId('column');

export const [screenIdKey, screenId] = makeTestId('layout');

export const [cardIdKey, cardId] = makeTestId('card');

export const [postIdKey, postId] = makeTestId('post');

export const [navMenuId, navMenu] = makeTestId('navMenu');
