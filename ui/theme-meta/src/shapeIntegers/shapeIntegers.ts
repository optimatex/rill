import { int } from '../integersFactory';

export const [layout, layoutRule, setLayout] = int('layout');

export const [column, mainColumnRule, setMainColumn] = int('column');

export const [frame, frameRule, setFrame] = int('frame');

export const [font, fontRule, setFont] = int('font');

export const [outline, outlineRule, outlineFont] = int('outline');
