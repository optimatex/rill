import { int } from '../integersFactory';

export const [button, buttonRule, setButton] = int('button');

export const [buttonActive, buttonActiveRule, setButtonActive] =
  int('buttonActive');

export const [surface, surfaceRule, setSurface] = int('surface');

export const [card, cardRule, setCard] = int('card');

export const [panel, panelRule, setPanel] = int('panel');

export const [input, inputRule, setInput] = int('input');
