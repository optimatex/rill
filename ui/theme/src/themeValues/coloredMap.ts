import {
  cocaColor,
  cocaColorSideBar,
  cocaHeaderPack,
} from '_employee/theme-values-kit';

import dark from './mainDark';

export const mainColoredMap = {
  dark,
  light: cocaColor,
};

export const barColoredMap = {
  dark,
  light: cocaColorSideBar,
};

export const headerColoredMap = {
  dark: cocaHeaderPack,
  light: cocaHeaderPack,
};
