import { themeFactory } from '_employee/theme-meta';
import { shapePack } from '_employee/theme-values-kit';

import { mainColoredMap } from '../themeValues';

export const mainThemeFactory = themeFactory(shapePack, mainColoredMap);
